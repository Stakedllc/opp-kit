const repo_url = 'https://github.com/Stakedllc/opp-kit';
const CONST = {
    repo : {
        url: repo_url,
        template: `${repo_url}.git`,
        opportunity: {
            blob: `${repo_url}/blob/master/contracts/MyOpportunity.sol`,
            raw: 'https://raw.githubusercontent.com/Stakedllc/opp-kit/master/contracts/MyOpportunity.sol'
        }
    }
}

export default CONST;